# =============================================================================
# Trusted ClawMon Dashboard — Environment Variables
# =============================================================================
# All variables must be prefixed with VITE_ to be available in the browser.
# These are baked into the build at compile time.
# =============================================================================

# API Server URL (required for production)
# The full URL of your deployed API server, without trailing slash.
# Leave empty for local development (Vite proxy handles /api and /ws).
# Examples:
#   https://trusted-clawmon-api.up.railway.app
#   https://api-clawmon.onrender.com
VITE_API_URL=

# Monad RPC URL (optional — public default is used)
VITE_MONAD_RPC_URL=https://testnet.monad.xyz/v1

# WalletConnect Project ID (get from https://cloud.walletconnect.com)
# Required for WalletConnect v2 / MetaMask wallet connections.
VITE_WALLETCONNECT_PROJECT_ID=
